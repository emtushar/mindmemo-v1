<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mind Memo</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="w-full min-h-screen bg-zinc-900 text-white p-8 flex flex-col">
    <h1 class="text-3xl text-center">
      Mind Memo :
      <span class="text-2xl px-3 py-2 bg-zinc-700 rounded-md text-orange-300"
        >streamline your thoughts</span
      >
    </h1>

    <form
      action="/create"
      method="post"
      class="flex gap-10 flex-col rounded-lg p-10 mt-8 w-[500px]"
    >
      <input
        class="bg-zinc-600 py-2 px-4 h-[70px] rounded-md outline-none text-2xl"
        type="text"
        placeholder="title goes here..."
        value=""
        name="title"
      />
      <textarea
        class="bg-zinc-600 py-2 px-4 rounded-md h-[250px] resize-none outline-none text-2xl"
        name="details"
        id=""
        placeholder="note goes here..."
      ></textarea>
      <button class="bg-blue-400 py-2 px-4 rounded-md w-20">Add</button>
    </form>
    <% if(dataArray.length >0){ %>
    <div class="mt-5 flex gap-20 flex-wrap">
      <% dataArray.forEach((item)=>{ %>
      <div
        class="bg-zinc-500 p-6 rounded-md min-h-[250px] max-h-[380px] w-80 flex flex-col gap-5"
      >
        <h2 class="text-3xl text-zinc-100 font-semibold"><%= item.title%></h2>
        <div class="flex justify-between gap-4">
          <p class="text-xl text-zinc-300 tracking-wide truncate mb-3">
            <%= item.details%>
          </p>
          <a
            class="font-bold text-sm font-mono text-white tracking-wide rounded-md"
            href="/<%= item.title.replace(' ','') %>"
          >
            Read More
          </a>
        </div>

        <div class="flex justify-between p-4 mt-6">
          <a
            href="/update/<%= item.title.replace(' ','') %>"
            class="bg-black text-base texl-zinc-500 px-4 py-2 rounded-md"
          >
            Update
          </a>
          <a
            href="/delete/<%= item.title.replace(' ','') %>"
            class="bg-black text-base texl-zinc-500 px-4 py-2 rounded-md"
          >
            Delete
          </a>
        </div>
      </div>
      <% }) %>
    </div>
    <% }else{%>
    <div class="text-xl text-zinc-300">No tasks are here ...</div>
    <% }%>
  </body>
</html>
